The time complextity of add_block is O(1), for n blocks the total time complexity would be O(n)

The total space complexity would be O(n) as well.

I used hashlib to calculate the hash of all data and datetime to calculate the current GMT